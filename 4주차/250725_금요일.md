## 오늘 진행한 내용
```
DROP DATABASE IF EXISTS box_office2;
CREATE DATABASE box_office2;
USE box_office2;

CREATE TABLE `movie_importer` (
	`movie_id`	INT UNSIGNED	NOT NULL,
	`company_id`	INT UNSIGNED	NULL
);

CREATE TABLE `movie_production` (
	`movie_id`	INT UNSIGNED	NOT NULL,
	`company_id`	INT UNSIGNED	NULL
);

CREATE TABLE `movie_genre` (
	`genre_id`	INT UNSIGNED	NOT NULL,
	`movie_id`	INT UNSIGNED	NOT NULL
);

CREATE TABLE `country` (
	`country_id`	INT UNSIGNED AUTO_INCREMENT PRIMARY KEY	NOT NULL,
	`name`	VARCHAR(50)	NULL
);

CREATE TABLE `director` (
	`director_id`	INT UNSIGNED AUTO_INCREMENT PRIMARY KEY	NOT NULL,
	`name`	VARCHAR(50)	NULL
);

CREATE TABLE `movie_director` (
	`movie_id`	INT UNSIGNED	NOT NULL,
	`director_id`	INT UNSIGNED	NOT NULL
);

CREATE TABLE `rating` (
	`rating_id`	INT UNSIGNED AUTO_INCREMENT PRIMARY KEY	NOT NULL,
	`name`	VARCHAR(20)	NULL
);

CREATE TABLE `genre` (
	`genre_id`	INT UNSIGNED AUTO_INCREMENT PRIMARY KEY	NOT NULL,
	`name`	VARCHAR(50)	NULL
);

CREATE TABLE `boxoffice` (
	`movie_id`	INT UNSIGNED AUTO_INCREMENT PRIMARY KEY	NOT NULL,
	`screens`	SMALLINT UNSIGNED	NULL,
	`revenue`	FLOAT	NULL,
	`audience`	INT UNSIGNED	NULL
);

CREATE TABLE `movie_distributor` (
	`movie_id`	INT UNSIGNED	NOT NULL,
	`company_id`	INT UNSIGNED	NOT NULL
);

CREATE TABLE `company` (
	`company_id`	INT UNSIGNED AUTO_INCREMENT PRIMARY KEY	NOT NULL,
	`name`	VARCHAR(50)	NULL
);

CREATE TABLE `movie` (
	`movie_id`	INT UNSIGNED AUTO_INCREMENT PRIMARY KEY	NOT NULL,
	`title`	VARCHAR(50)	NULL,
	`release_date`	DATE	NULL
);

CREATE TABLE `movie_country` (
	`country_id`	INT UNSIGNED	NOT NULL,
	`movie_id`	INT UNSIGNED	NOT NULL
);

CREATE TABLE `movie_rating` (
	`rating_id`	INT UNSIGNED	NOT NULL,
	`movie_id`	INT UNSIGNED	NOT NULL
);

ALTER TABLE `movie_importer` ADD CONSTRAINT `PK_MOVIE_IMPORTER` PRIMARY KEY (
	`movie_id`,
	`company_id`
);

ALTER TABLE `movie_production` ADD CONSTRAINT `PK_MOVIE_PRODUCTION` PRIMARY KEY (
	`movie_id`,
	`company_id`
);

ALTER TABLE `movie_genre` ADD CONSTRAINT `PK_MOVIE_GENRE` PRIMARY KEY (
	`genre_id`,
	`movie_id`
);


ALTER TABLE `movie_director` ADD CONSTRAINT `PK_MOVIE_DIRECTOR` PRIMARY KEY (
	`movie_id`,
	`director_id`
);


ALTER TABLE `movie_distributor` ADD CONSTRAINT `PK_MOVIE_DISTRIBUTOR` PRIMARY KEY (
	`movie_id`,
	`company_id`
);


ALTER TABLE `movie_country` ADD CONSTRAINT `PK_MOVIE_COUNTRY` PRIMARY KEY (
	`country_id`,
	`movie_id`
);

ALTER TABLE `movie_rating` ADD CONSTRAINT `PK_MOVIE_RATING` PRIMARY KEY (
	`rating_id`,
	`movie_id`
);

ALTER TABLE `movie_importer` ADD CONSTRAINT `FK_movie_TO_movie_importer_1` FOREIGN KEY (
	`movie_id`
)
REFERENCES `movie` (
	`movie_id`
);

ALTER TABLE `movie_importer` ADD CONSTRAINT `FK_company_TO_movie_importer_1` FOREIGN KEY (
	`company_id`
)
REFERENCES `company` (
	`company_id`
);

ALTER TABLE `movie_production` ADD CONSTRAINT `FK_movie_TO_movie_production_1` FOREIGN KEY (
	`movie_id`
)
REFERENCES `movie` (
	`movie_id`
);

ALTER TABLE `movie_production` ADD CONSTRAINT `FK_company_TO_movie_production_1` FOREIGN KEY (
	`company_id`
)
REFERENCES `company` (
	`company_id`
);

ALTER TABLE `movie_genre` ADD CONSTRAINT `FK_genre_TO_movie_genre_1` FOREIGN KEY (
	`genre_id`
)
REFERENCES `genre` (
	`genre_id`
);

ALTER TABLE `movie_genre` ADD CONSTRAINT `FK_movie_TO_movie_genre_1` FOREIGN KEY (
	`movie_id`
)
REFERENCES `movie` (
	`movie_id`
);

ALTER TABLE `movie_director` ADD CONSTRAINT `FK_movie_TO_movie_director_1` FOREIGN KEY (
	`movie_id`
)
REFERENCES `movie` (
	`movie_id`
);

ALTER TABLE `movie_director` ADD CONSTRAINT `FK_director_TO_movie_director_1` FOREIGN KEY (
	`director_id`
)
REFERENCES `director` (
	`director_id`
);

ALTER TABLE `boxoffice` ADD CONSTRAINT `FK_movie_TO_boxoffice_1` FOREIGN KEY (
	`movie_id`
)
REFERENCES `movie` (
	`movie_id`
);

ALTER TABLE `movie_distributor` ADD CONSTRAINT `FK_movie_TO_movie_distributor_1` FOREIGN KEY (
	`movie_id`
)
REFERENCES `movie` (
	`movie_id`
);

ALTER TABLE `movie_distributor` ADD CONSTRAINT `FK_company_TO_movie_distributor_1` FOREIGN KEY (
	`company_id`
)
REFERENCES `company` (
	`company_id`
);

ALTER TABLE `movie_country` ADD CONSTRAINT `FK_country_TO_movie_country_1` FOREIGN KEY (
	`country_id`
)
REFERENCES `country` (
	`country_id`
);

ALTER TABLE `movie_country` ADD CONSTRAINT `FK_movie_TO_movie_country_1` FOREIGN KEY (
	`movie_id`
)
REFERENCES `movie` (
	`movie_id`
);

ALTER TABLE `movie_rating` ADD CONSTRAINT `FK_rating_TO_movie_rating_1` FOREIGN KEY (
	`rating_id`
)
REFERENCES `rating` (
	`rating_id`
);

ALTER TABLE `movie_rating` ADD CONSTRAINT `FK_movie_TO_movie_rating_1` FOREIGN KEY (
	`movie_id`
)
REFERENCES `movie` (
	`movie_id`
);
```

## 오늘의 포인트
- ERD CLOUD를 통해 직접 그리고 MySQL 코드로 변환함